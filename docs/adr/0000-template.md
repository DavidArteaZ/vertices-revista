# 0000 — ADR template (delete or replace)

This is a placeholder so `docs/adr/` exists from day one. Replace with your first real ADR (`0001-slug.md`).

Format: see `ADR-FORMAT.md`. Most ADRs are one paragraph: context + decision + why.
